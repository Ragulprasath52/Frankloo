{
  "name": "frankloo-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --legacy-peer-deps && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently --kill-others \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:generate": "npm run db:generate --prefix backend",
    "db:seed": "npm run db:seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
